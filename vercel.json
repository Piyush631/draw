{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install -g pnpm@8.6.12 && pnpm install --no-frozen-lockfile",
        "buildCommand": "pnpm build"
      }
    }
  ],
  "env": {
    "NODE_VERSION": "20.x"
  }
} 