{
  "name": "excel-draw",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "postinstall": "pnpm --filter database exec prisma generate",
    "vercel-build": "pnpm --filter excel-draw-frontend@workspace build"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.4.4",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@8.6.1",
  
    "engines": {
  "pnpm": "8.6.0", 
  "node": "18.x"  
}

  
}
